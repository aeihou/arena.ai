# MyPrompts

Append-only developer prompts recorded by BeforeThinking.Add(new Prompt).
Each entry is the verbatim request. Runtime Git identity is not recorded here.

## 1 — 2026-08-19

```
new selfExecutingScript from pseudocode: mkDirNameOfFolder;nameOfFolder.new(nameoFFolder.Readme)
```

## 2 — 2026-08-19

```
export my prompt.log detailed
```

## 3 — 2026-08-19

```
newFolder(/AGENTS/.user/); Keep there my ++**[[MyPrompts.md](http://MyPrompts.md)]([http://MyPrompts.md)**++().Keep](http://MyPrompts.md)**++().Keep) all mypromts sent in this session; and generate a function [BeforeThinking.MyPrompts.md](http://BeforeThinking.MyPrompts.md)().Add(new Prompt);
```
